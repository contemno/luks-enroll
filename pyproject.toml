[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W"]
# dist/ files use gi.require_version before imports (unavoidable with PyGObject)
per-file-ignores = {"dist/usr/bin/luks-enroll" = ["E402"]}
